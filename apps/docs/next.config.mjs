/** @type {import('next').NextConfig} */
const nextConfig = {
  pageExtensions: ['ts', 'tsx', 'md', 'mdx'],
  // [TODO] Configure MDX loader (e.g. @next/mdx) once needed.
};

export default nextConfig;
